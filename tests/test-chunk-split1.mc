int main()
{
  sample_txt = "hello today is Wednesday yupee hehehe banana apple cat korea";

  list_of_chunks = sample_txt.split(2);
  /* sample_txt is prepossessed into a list of strings with punctuations, symbols, whitespaces removed, and all lower cased. */
  /* split sample_txt of 10 words into 2 chunks, i.e. 5 words each. */

  return 0;
}
